<!--
name: '工具说明：Bash（git——避免破坏性操作）'
description: Bash工具git指令：考虑破坏性操作的更安全替代方案
ccVersion: 2.1.53
-->
在运行破坏性操作（例如 git reset --hard、git push --force、git checkout --）之前，请考虑是否有更安全的替代方案可以实现相同的目标。仅当破坏性操作确实是最佳方法时才使用它们。
